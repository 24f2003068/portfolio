name: IITM Assignment

on:
workflow_dispatch:

jobs:
test:
runs-on: ubuntu-latest

```
steps:
  - name: 24f2003068@ds.study.iitm.ac.in
    run: echo "Hello, world!"

  - name: Verify
    run: echo "Workflow completed"
```
